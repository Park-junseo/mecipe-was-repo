apiVersion: v2
name: kafka
description: Kafka for Mecipe (Confluent Platform)
type: application
version: 0.2.0
appVersion: "7.6.0"
# Dependencies removed - Zookeeper should be deployed independently
# Zookeeper is referenced via dependencies.zookeeper.name and namespace in values.yaml
