# Default values for mecipe-was
global:
  dockerRegistry: ""
  imagePullSecrets: []

name: mecipe-was
namespace: app

nodeSelector:
  node-role: app

image:
  repository: "mecipe-api-server"
  tag: "latest"
  pullPolicy: Always

replicaCount: 1

ingress:
  enabled: true
  host: ""
  tls:
    enabled: true
  tlsSecret: "mecipe-was-tls"
  annotationsSettings:
    certManager.enabled: true
    https.redirect.enabled: true
    securityHeaders.enabled: true
    websocket.enabled: true

service:
  type: ClusterIP
  ports:
    http: 4000
    socket: 4100

env:
  nodeEnv: production
  port: "4000"
  socketPort: "4100"
  redisUrl: "redis://redis-master.app.svc.cluster.local:6379"

postgres:
  username: ""
  password: ""
  database: ""
  hosts: ""

elasticsearch:
  hosts: ""
  username: ""
  password: ""

persistence:
  enabled: true
  size: 10Gi
  storageClass: ""

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

livenessProbe:
  httpGet:
    path: /hello
    port: 4000
  initialDelaySeconds: 60
  periodSeconds: 30

readinessProbe:
  httpGet:
    path: /hello
    port: 4000
  initialDelaySeconds: 30
  periodSeconds: 10

# Secrets (should be set via --set or values file)
secrets:
  jwtSecret: ""
  secretLoginCrypto: ""
  apiKey: ""
  buildApiKey: ""
  couponSecret: ""
  productSecret: ""

